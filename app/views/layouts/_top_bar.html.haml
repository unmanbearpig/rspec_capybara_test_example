%nav.top-bar(data-topbar)
  %ul.title-area
    %li.name
      %h1
        %a{href: '/'}
          = content_for?(:title) ? yield(:title) : "Simple rails twitter"
  %section.top-bar-section
    %ul.right
      %li.login-link
        = render 'layouts/login_link'
